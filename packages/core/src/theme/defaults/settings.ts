import type { ThemeDefaults } from "../createTheme";

export const defaultSettings: Required<ThemeDefaults> = {
    radius: "md",
    primaryColor: "cloud",
    primaryShade: 5,
    autoContrast: true
};
